export const DEFAULT_TERMINAL_COMMAND_TIMEOUT_SECONDS = 1800
export const MIN_TERMINAL_COMMAND_TIMEOUT_SECONDS = 60

/** Default wait before a foreground command is handed to background tracking. */
export const DEFAULT_TERMINAL_COMMAND_HANDOFF_SECONDS = 10
/** Minimum configurable foreground-to-background handoff wait in seconds. */
export const MIN_TERMINAL_COMMAND_HANDOFF_SECONDS = 1
